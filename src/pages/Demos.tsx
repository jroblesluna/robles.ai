import DemosCatalog from "@/components/DemosCatalog";

/**
 * Dedicated /demos page — shows the full AI demos catalog (no limit).
 * The DemosCatalog component already renders its own header and section.
 */
export default function Demos() {
  return <DemosCatalog />;
}
